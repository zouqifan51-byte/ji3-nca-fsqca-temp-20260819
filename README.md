# Temporary JI3 NCA + fsQCA runner

This temporary public repository contains only the GitHub Actions controller. The analysis code remains in the private source repository and is checked out at an exact commit during the run. Raw row-level data are never committed here.
